enum ActivityTypeEnum {
  Picture, // 1
  Question, // 2
}

enum WidgetStatusEnum {
  Empty,
  EmptyResult,
  Result,
  Error,
  Loading,
  Default,
}

enum AppEnvironment {
  Local,
  Development,
  Production,
}