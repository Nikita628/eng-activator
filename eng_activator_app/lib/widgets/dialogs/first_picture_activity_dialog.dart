import 'package:flutter/material.dart';

class FirstPictureActivityDialog extends StatelessWidget {
  const FirstPictureActivityDialog({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}